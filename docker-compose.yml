version: '2'
services:
  weba:
    build: .
    expose:
      - 8080
    ports:
      - "8080:8080"
    links:
      - db

  db:
    image: postgres
    volumes:
      - ~/work/data/nufito_db/db_data:/var/lib/postgresql/data
    expose:
      - 5432
    ports:
      - "1234:5432"
